export interface Action {
  id: string;
  label: string;
  icon: string;
}
export interface Button {
  id: string;
  label: string;
  icon: string;
  class: string;
}

export interface Column {
  name: string;
  label: string;
}
